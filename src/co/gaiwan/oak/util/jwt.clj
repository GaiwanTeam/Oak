(ns co.gaiwan.oak.util.jwt
  "Unopionated helpers for dealing with JWTs

  JWTs are JSON Web Tokens, consisting of three parts, separated by dots, each
  of them base64 encoded. A header, payload, and signature.")
