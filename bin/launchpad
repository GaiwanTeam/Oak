#!/usr/bin/env bb

(require '[lambdaisland.launchpad :as launchpad])

(launchpad/main
 {:java-args ["-Dclojure.main.report=stderr"]
  :pre-steps
  [(launchpad/run-process {:cmd '[docker compose up -d --no-recreate]})]})

;; Local Variables:
;; mode:clojure
;; End:
